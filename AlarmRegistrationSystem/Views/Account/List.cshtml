@model Models.ViewModels.ListViewModel<Models.ViewModels.UserListViewModel>
@{
    Layout = "_Layout";
    ViewData["Title"] = "Wyswietl Uzytkownikow";
}
<div class="row col s12 main">
    <form id="Form" class="search-form" asp-controller="Account" asp-action="ListUsers" data-container="UsersTable">
        <input id="CurrentPage" type="hidden" name="currentPage" value="1" />
        <input id="DeleteUserName" name="uniqueUserName" type="hidden" value="" />
        <div class="row col s12 m12 l8 push-l2">
            <div class="col s6 l4">
                <div class="input-field col s12">
                    <select id="StateOption" name="searchRole">
                        <option value="" selected>Wszystkie</option>
                        <option value="Administrators">Administratorzy</option>
                        <option value="Mechanics">Mechanicy</option>
                        <option value="Employes">Pracownicy</option>
                    </select>
                    <label>Typ Konta</label>
                </div>
            </div>
            <div class="col s6 push-l2">
                <div class="row input-field">
                    <i class="fas fa-search prefix"></i>
                    <input id="icon_prefix" type="text" class="validate" name="searchText">
                    <label for="icon_prefix">Wyszukaj</label>
                </div>
            </div>
        </div>
    </form>
    <div id="UsersTable" class="row col s12 l8 push-l2">
        @await Html.PartialAsync("Partial/_UsersTable")
    </div>
    <div class="row center-align">
        <div class="col s12">
            <a class="btn waves-effect waves-light" asp-action="CreateAccount" asp-controller="Account" asp-route-returnurl="@ViewContext.HttpContext.Request.Path">Dodaj Uzytkownika</a>
        </div>
    </div>
</div>

@section scripts{
    <script src="~/Scripts.js" type="text/javascript"></script>
    <script>
        $(document).ready(function () {
            $('select').formSelect();
            $('.dropdown-trigger').dropdown();
            searchActivator('Form');
            $('.modal').modal();
        });
        $(document).ajaxComplete(function () {
            $('.dropdown-trigger').dropdown();
            $('.modal').modal();
        });
        $(document).on('click', '.DoAndReload', function () {
            passAttr(this);
        });
        $(document).on('click', '.pageButton', function () {
            changeCurrentPage(this);
        });

        $(document).on('click', '.modal-true', function () {
            $('#modal1').modal("close");
            deleteActivator(this, "/account/DeleteUser");
        });

    </script>
}
